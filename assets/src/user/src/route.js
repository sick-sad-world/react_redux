import React from 'react';
import { Route } from 'react-router';
import Container from './containers/container.jsx';

export default (<Route path='/settings' component={Container} label='Settings' icon='cog' />);
